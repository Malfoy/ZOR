[package]
name = "xor_filter"
version = "0.1.0"
edition = "2021"

[lib]
name = "xor_filter"
path = "src/lib.rs"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = "symbols"
panic = "abort"
